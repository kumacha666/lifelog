# LifeLog — 開発コンテキスト

## アプリ概要
日常の細かな記録を毎日つけるライフログ＋ライフコーチ PWA。

## 設計思想
- **カテゴリ駆動**：腕時計・靴・食事など、ユーザーが自分でカテゴリを定義する
- **アイテム事前登録**：持ち物リストを登録しておき、記録時はタップで選ぶだけ
- **最小限でも成立**：腕時計だけ記録したい人はそれだけでOK、多機能にも対応
- **ローカルファースト**：データは localStorage に保存、サーバー不要
- **PWA**：スマホのホーム画面に追加してアプリとして使用可能

## カテゴリタイプと特徴
- `fashion`：バリエーション率・未使用アイテム提案・最多使用アイテム
- `food`：カロリー記録・平均カロリー評価・食品多様性
- `supplement`：連続記録ストリーク・30日ドット表示
- `purchase`：金額記録・月間支出集計
- `custom`：自由記録

## 技術構成
- シングルファイル HTML（index.html）+ manifest.json + sw.js
- フレームワーク不使用、バニラ JS
- データ構造：`{ cats: [], logs: { "YYYY-MM-DD": { catId: { v, kcal, yen, note } } } }`
- localStorage キー：`lifelog_v2`

## ユーザーの環境
- スマホ（Android Chrome）でダークモード使用
- GitHub Pages でホスト：`https://kumacha666.github.io/lifelog/`

## 既知の課題・対応済み
- ダークモードでチップ（選択肢ボタン）の文字・背景が見づらい → dark mode CSS override で対応中

## 今後やりたいこと（未実装）
- ユーザーが言及していた追加要望があればここに追記
